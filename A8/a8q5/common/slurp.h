
// slurp(s, maxlen) reads up to maxlen characters from
//   input and stores them in s
// requires: s is an array with size at least maxlen + 1
//           maxlen > 0
// effects:  reads from input
//           modifies s
void slurp(char *s, int maxlen);
